class CreditDetails {
  String firstText, firstDesc, secondText, secondDesc;
  CreditDetails(
      {required this.firstDesc,
      required this.firstText,
      required this.secondDesc,
      required this.secondText});
}
